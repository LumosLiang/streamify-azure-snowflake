version: 2

sources:
    - name: staging
      database: "{{ env_var('SNOWFLAKE_DATABASE', 'STREAMIFY') }}"
      schema: STREAMIFY_STG
      tables:
        - name: listen_events
        - name: page_view_events
        - name: auth_events
        - name: songs
